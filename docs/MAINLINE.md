# MAINLINE — 日更主线

> 最后更新:2026-09-18。**先读本节**;下面"历史"部分是 09-05 以前的记录,只供查证。
> 标注:**[已验证事实]** 有日志/命令为证 · **[Agent 提案]** 未经你批准 · **[Boss 决定]** 你已拍板。

## 现在的主线(2026-09-18)

发布流程已经稳定:每天自动跑到网站草稿,你在 Telegram 审阅,再手动发公众号。
**主线是内容的数量和质量**:量不够(每期能写好的条目少),质量不稳(编造、刷屏、多个模型混写)。

### 当前最大瓶颈:解读写不满 [已验证事实]

- 每晚 60 条候选,**只有约 34 条解读成功**(09-17 finalize:34/60,其中 27 条来自缓存)。
- 原因是 agnes 被限流:每分钟我们发出 15–25 次请求,**每分钟最多只成功 2 次**,从没超过。
- 你说限额是每分钟 20 次,这和"最多成功 2 次"对不上,所以真正卡住的限额还不清楚
  (最可能是按 token 计,每次调用都预扣了 8000 token;未证实)。
- 证据:Collect 运行 35212772595 日志,按分钟统计 `apihub.agnes-ai.com` 的请求与 200。

### 这一轮做完的事(09-10 → 09-18,全部已合并)

| 问题 | 做了什么 | PR |
|---|---|---|
| agnes 限流 / 推理预算烧穿 / 空响应 | 429 退避重试;预算烧穿时翻倍重试一次;空响应报清楚原因 | #184 #189 |
| 每条被重复解读、白烧额度 | 解读按链接缓存,一天只写一次,finalize 复用 | #191 |
| 编造(如把功能提示写成"推出") | 一个原文锚点都没有的解读不采用 | #193 |
| 看不出每条是谁写的 | 记录每条的写作模型 | #185 |
| 同一家公司跨渠道刷屏 | X + 博客 + GitHub 合计每家最多 2 条(9 家) | #200 |
| 你从不保留的来源 | 停用 hackernews、cline-gh、ollama-gh;拉黑 higgsfield 两个账号;lobe-chat-gh、google-ai 降权 | #204 |
| 没有按来源的反馈数据 | 手动 workflow `Decision Stats` 出保留率表;决策每晚落库 | #195 #198 #201 #202 |
| finalize 启动晚 | cron 提前两小时 | #187 |

| 一期里混着两三个模型的文风 | **[Boss 决定] agnes 优先**:agnes 写了就只发它写的;一条没写才退回"谁写得最多发谁" | #206 |

一期一模型的代价:其他模型写的条目不发、也不重写。09-17 那期丢 2 条(34 → 32)。

### 等你决定的事

**① 要不要先查清 agnes 的真实限额**(推荐:要)
- 做什么:429 时把响应头(`x-ratelimit-*`)和响应体记进日志。一次 collect 就能知道是按请求还是按 token 限、额度多大、多久恢复。
- 为什么先查:不查清就调参数是在猜。如果是按 token 限,把 `max_tokens` 从 8000 降下来就能多写好几倍;如果是按请求限,要做的是客户端限速和减少重试。两条路的改法完全不同。
- 代价:一个很小的 PR,不改任何行为。
- 不做的后果:每期继续只有 30 多条解读。

**② x:pixai_official 这类"保留率高但几乎全靠跳过"的来源要不要砍**
- 数据:看过 9 次,保留 1、丢弃 0、跳过 8,保留率算出来是 100%,实际上你基本不看。
- 选项:砍 / 降权 / 不动。

### 你需要知道的几件事 [已验证事实]

- **你不在 Telegram 点保留,当晚就是空稿**。确认门只放行点了保留的条目;09-12 到 09-14 三晚都是空稿。
- **故事线合并从上线起一次都没合并过**。它只认"带版本号且同一天"的两条,329 条推文里只有 22 条带版本号。
  没修它,而是用公司封顶(#200)解决刷屏;要修它需要额外调模型,现在额度不够。
- **按来源的保留率随时可看**:Actions → `Decision Stats` → Run workflow,表在运行日志里。
  09-17 的结果:hf-papers 87%(81 次)、hf-models 94%、openai 79%;higgsfield 0%(16 次)、cline-gh 1/13。

### 还没做、没排期的

| 事项 | 状态 |
|---|---|
| "必须带社区信号才收" | [Agent 提案] 不建议一刀切:apple-ml、deepmind 没有社区信号也是 100% 保留 |
| 公众号外链图能否正常显示(#124) | 一直没验证 |
| 跨天语义去重(#147) | 向量库从来没接上 |
| 手动重跑已发布的刊物会重复(#160 后半) | 未修 |

---

# 历史(2026-09-05 及以前)


> 完整任务清单仍在 `docs/KANBAN.md`,这份文档只挑**当前真正卡住主线的最小必要集合**,按优先级从上到下做,做完一条再看下一条。
> 最后更新:2026-09-05(凌晨暴露的三个问题当天全部修完; 主线剩'触发时机'与'重跑去重'两件)。

---

## 2026-09-05 现状:三条修好了,三条新问题浮出来

09-04 那次 finalize 是当天所有改动第一次一起真实生效, 结果分两面。

**兑现的**:
- 并发 —— **40-60 分钟 → 26 分钟**(22:52→23:18), 且 interpret **100/100 全成功、0 回退**(历来最好), 并发没伤到质量。
- 公众号版 —— `content/wechat/2026-09-04.md` 自动产出, **人第一次不用手工转换**; 目录/裸 URL/分隔线/标签清洗全部就位。
- 推送 —— `Commit draft` 这次成功。此前 09-01/02/03 三晚的定时运行全部失败, 草稿全靠手动补跑, **从外面看完全像是自动跑通了**。

**同一次跑暴露的三个问题**(详见 KANBAN §2/§3):

1. ~~日期命名撞车(#160)~~ **已修 #165** —— 按发布日命名(伦敦午夜后发 = 北京早上八点)。剩一半: 手动重跑一份已发布的刊物仍会重复。
2. ~~配额砍掉用户 keep 的条目(#132)~~ **已修 #164** —— 配额只在装不下时才施加。
3. ~~标题连挂三晚(#161)~~ **已修 #163** —— 退回 2 个事件, 算术由测试钉死。

**三条当天全部修完并合并。** 下一份刊物(09-05)是它们第一次一起生效。

主线因此从"减少人力"转到"**减少错发**":格式问题基本收进代码了, 现在扣分的是**内容选择**——重复、漏掉大新闻、标题拿不出手。

---

## 主线已经变了(2026-09-04 重写)

**原目标"能不能切日更"已经不再是问题——用户从 2026-08-31 起每天真发公众号,连发了几天。** 08-27 版列的三条前置(故事线合并 #119、实体 fact-check #117、GitHub Releases 时效性 #105)全部 SHIPPED,且经过用户连续几天逐条真实审阅。

所以主线从"**能不能日更**"换成了"**日更这件事现在每天要花多少人力、出多少错**"。当前形态:

- 流水线自动跑到**网站版草稿**为止(collect ×8/天 → TG 人审 → finalize → Hugo Pages),这部分是自动的;
- **公众号那份 md 是我每天手工从网站版转换的**——加目录、清标签、补标题摘要、配图核对;
- 用户手工编辑后粘贴进 doocs/md 发布。

出错的位置高度集中在中间那段手工环节。下面三条按"减少人力 + 减少出错"排。

---

## ~~P0~~:finalize 跑一次要 40-60 分钟 — **已修 #152/#153,实测 2.2x**

2026-09-03/04 连续两晚,用户等到深夜,原话"这他妈卡在哪里了"、"每天都这样,那我还发不发了今天"。

根因两条叠加,**其中一条是我造成的**:

1. 我把 `card_pool_limit` 50→100 时**只算了 LLM 调用次数的成本,没算墙钟时间**。`interpret()` 是纯顺序 for 循环,一条一条等 LLM 返回,100 条就是 100 次串行往返。
2. 同期 ModelScope / agnes 本身不稳(400 / 500 / 超时),每条要重试更久。

**修法不是把发卡池缩回去**——那会把刚救回来的 X 覆盖率(2/50 → 20/50)搭进去。正确修法是让 interpret 并发跑:调用次数与成本完全不变,墙钟从「N × 单条延迟」降到「N/并发 × 单条延迟」。

**2026-09-04 晚已合并(#153)**,`concurrency` 默认 4 走 `config/interpret.yaml`。真实 provider 实测(8 条同一批跑两遍):

| 并发 | 墙钟 | 解读成功 | 顺序一致 |
|---|---:|---:|---|
| 1(原行为) | 524.5s | 6/8 | ✅ |
| 4(新默认) | 238.8s | 7/8 | ✅ |

**2.2x,不是设置暗示的 4x**:8 条配 4 并发是两波,每波只能跟最慢那条一样快,而单条延迟波动大(均值 65s,还要穿模型链重试),样本小的时候尾部主导。100 条池子波次多了会摊平,应该更接近上限——**这是推断不是实测**。

按这个比例,40-60 分钟落到 **18-27 分钟**:好转了,不算好。下一个杠杆是把 `concurrency` 调高,该看生产 429 数据再调,不要拍脑袋。并发没让失败变多(6/8 → 7/8),但 8 条样本对限流说明不了什么,**真正的检验是下一次生产跑**。

---

## ~~P1~~:公众号版整份是手工转换的 — **已修 #155/#156(2026-09-04 晚)**

原问题:目录规则写进了 spec,但流水线只渲染网站版,公众号那份每天人工转换。**漏目录、漏标签清洗、漏摘要补全——出错的每一次都是手工那一步,不是流水线。**

`render_wechat()` 现在直接产出 `content/wechat/<date>.md`(#155),标签清洗 `clean_wechat_tags()` 一并落地(#156),spec 2026-08-31 全部实现完毕。四处与网站版的差异:无 front matter/页脚/H1、有目录、参考用裸 URL、剔除解读回退条目;条目正文走同一条渲染路径,**两个渲染器必然漂移,而漂移正是手工那步反复出错的原因**。

实测:真实报告(09-03,8 条)六项结构检查全过,目录/正文/参考三处编号一致;标签清洗拿 4 天共 75 个真实标签跑,改动 4 个(全是微信本来就会截断的),其余 71 个未动。

**人现在只做内容判断,不做格式搬运。** 明天那次 finalize 是它第一次真实生效。

---

## P1:公众号到底渲不渲染外链图,至今没验证(#124)

配图质量刚刚修到位(#148/#150:8 条里 4 条有图且 4 张全是真图)。但**如果公众号根本不渲染外链图,这些工作在公众号侧等于零**。

这条不需要开发,只需要下一次发布时拿真实文章看一眼——优先级因此反而上升。

---

## 历史:08-27 版的当时最重要任务(均已 SHIPPED)

> **2026-08-27 更正**:昨天曾把"interpret 模型链大范围失灵"当成新 P0 记在这里,查了 4 次真实 GitHub Actions 日志后发现是误判——那次"7 个模型全灭"是我自己当天用同一把 `MODELSCOPE_API_KEY` 连续跑了几轮本地诊断脚本,把配额打满触发 `429`,污染了信号,不是生产真的坏了。真实数据:4 次真实 run 的 `item_interpreted` 平均成功率 ~87%(46ok/4fallback、46ok/4fallback、39ok/11fallback、43ok/7fallback)。已降级为 P2,详见 KANBAN。**教训**:诊断"是否失灵"要查真实生产日志,不要用自己再打一轮真实请求去验证。

#### 内部 source slug 泄漏成"事实"——同时是编造 bug 和显示 bug(已 SHIPPED)

2026-08-15 发现,2026-08-27 SHIPPED(#102):`src/prompts/interpret_item.md` 把 `item.source`(内部配置名,如 `x-ai-company`)原样喂给 LLM 当"来源"字段,LLM 把 "x-ai-company" 联想成真实公司 "xAI",把 NovelAI(`@novelaiofficial`)的推文写成"xAI发布"——真实发生的一起编造案例。同一个字段也曾被直接渲染成审阅卡片上的可见来源链接文字。已修:`x_list` 聚合类来源改喂安全通用标签,非聚合来源(本来就是真实机构名)不动;审阅卡片锚文本改用链接域名。拿真实案例过production 模型链验证过,不再泄漏内部 slug。

---

### 第二关:内容风格/钩子(决定"读者愿不愿意读完") — 已 SHIPPED(#115 内容确定性+标题钩子、#119 故事线合并)

用户原话:"信息总结的内容也有问题(不规范、风格不强、让人读了不会想继续读完,没有让读者感觉到对他们有利)"。这不是一条孤立 bug,是 KANBAN 里已经记录的几条 D组/P1 待办的同一根因——都是 `src/prompts/interpret_item.md` + `references/editorial-and-format-sop.md` 没有充分约束住的输出质量问题。建议合并成一次 `/brainstorm` 处理,而不是逐条打补丁:

- interpret 标题编造动作词(KANBAN,如"发布"没原文支撑)
- 打分没有"内容确定性/信息密度"维度,空洞内容能拿满分
- hf-papers 标题没把方法/模型名前置当钩子
- 同一故事线的跟进文章反复发布,挤占配额、显得重复

这几条加起来才是"风格不强/不想读完/没让读者觉得有利"这句话的具体所指。

---

### X / 内容具体问题(2026-08-27 用户点名,三条) — 三条均已 SHIPPED(#119 / #117 / #105)

#### 1. 同一事件多源重复:原发布 + 第三方"已支持"公告应合并成一条

例:A 公司发布某模型,随后多个第三方平台各自发"我们已支持该模型"——现状是每条都当独立新闻打分发布,读者看到的是同一件事被拆成好几条卡片。这跟 KANBAN #73(故事线合并)是同一类问题的具体案例,#73 原描述偏"多家媒体报道同一事件不同措辞",这次用户点出的是**原发布方 vs 下游第三方跟进支持公告**这种更具体的模式,需要在故事线合并设计时把这个模式也覆盖到,不是简单查标题相似度。

#### 2. 低置信度的公司名/模型名需要 fact-check,不能直接采信

跟本次已经查实的 `x-ai-company`→"xAI" 编造是同一类风险的更一般化表述:不只是 source slug 泄漏这一种触发方式,任何模型/公司名判断不确定的场合都需要一道"低置信度就别写死"的机制,而不是等撞名了才发现。这跟 KANBAN #69(内容确定性/信息密度打分维度)是同一根因但目标更明确——#69 原描述偏"给空洞内容降分",用户这次要的是**主动 double-check/fact-check 机制**,让 LLM 对自己不确定的实体名要么不写死、要么有二次校验,而不只是打分降权。两条建议合并到同一次 `/brainstorm` 一起设计。

#### 3. GitHub Releases 时效性:几天前的 release 当"今天"发(已 SHIPPED)

**已用代码验证根因**(不是猜测):`CollectionConfig.window_hours` 默认全局 72 小时,是为了不漏抓 paper/tool/blog 这类慢更新源才拉宽的,连带放行了 2-3 天前的 github_releases。**2026-08-27 SHIPPED(#105)**:新增 `window_hours_by_adapter`(默认 `{"github_releases": 48}`),按 adapter 查表取更紧的窗口,其余 adapter(paper/blog 等)不受影响。真实 collect 数据验证:比旧配置少收 11 条 48-72h 前的旧 release,37 条 ≤48h 的条目原样保留。

---

## 判断"能不能切日更"的完成标准

> **2026-09-04 复盘**:下面这套标准是为"敢不敢切日更"设计的,现在日更已经在跑,标准随之作废。保留原文备查,并按真实结果补上勾选。

- [x] source 泄漏 P0 已修(#102),仍需真实验证至少一天的发布内容没有再出现编造归属
- [x] interpret 模型链健康度已用真实日志核实(~87% 成功率,非大范围失灵),4 个已知死模型清理降级为 P2 顺手做
- [x] GitHub Releases 时效性已修(#105),几天前的 release 不再当"今天"发,真实数据验证过
- [x] 风格 + fact-check + 故事线合并的 brainstorm 至少有一版方案落地,并经过用户几天真实审阅确认"读得下去、信得过" — 三份 spec 全部 SHIPPED(#115/#117/#119),用户 08-31 起连续几天逐条真实审阅并发布
- [x] 没有新的编造类(P0/P1)问题被用户发现 — 09-01 至 09-04 用户逐条 review 挑出的问题全是**版式与配图**(美元符号、套话、空 body、图配错),**没有一条是编造类**

> 但出现了两类新问题,不在原标准的射程内,已升为当前主线:**耗时**(finalize 40-60 分钟)与**手工环节出错**(公众号版每天手工转换)。另有一个结构性缺口 #147:跨天去重只认相同链接,同一件事换个来源第二天会重发,而本该兜住的向量库**从来没接上**(`VectorStore` 只有 `upsert` 没有 `search`,实现是进程退出即丢的 `InMemoryVectorStore`)。

三条不全满足前不建议切日更——审阅频率一旦降低,编造类问题会更难被及时发现,风险比现在改的成本更高。
